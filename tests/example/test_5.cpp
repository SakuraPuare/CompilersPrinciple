// 测试文件 5: 注释处理和复杂表达式
int main() {
    // 这是单行注释
    int a, b, c;
    a = 10;
    
    /* 
     * 这是多行注释
     * 测试词法分析器对注释的处理
     */
    
    b = 5; // 行尾注释
    
    /* 注释测试 - 测试词法分析器对注释的处理 */
    
    // 测试复杂表达式
    c = (a + b) * (a - b) / 2;
    
    // 测试复杂条件判断
    if ((a > 0 && b > 0) || (a < 0 && b < 0)) {
        c = c + 1;
    }
    
    // 混合各种运算符
    c = c + ((a * b + c / 2) > 20 ? a : b);
    
    // 测试各种间距和格式
    a=1+2*3/4-5;
    b = 1 + 2 * 3 / 4 - 5;
    c=a+b*c/(a-b);
    
    return c;
} 